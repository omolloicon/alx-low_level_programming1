#include <stdio.h>
#include "main.h"
/**
 * main - entry point
 * @argc: - counter
 * @argv: - array
 * Return: 0 done
 */

int main(int argc __attribute__((unused)), char *argv[])
{
		printf("%s\n", *argv[0]);
	return (0);
}
