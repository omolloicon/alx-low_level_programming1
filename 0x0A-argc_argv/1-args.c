#include <stdio.h>

/**
 * main - prints argument number passed into it
 * @argv: array
 * @argc: count
 * Return: 0 done
 */

int main(int argc, char *argv[])
{
	(void) *argv[]
	printf("%d\n", argc);

	return (0);
}
